---
# Fake Loki ODF bucket configmap (used by loki-stack policy via fromConfigMap)
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-bucket-odf
  namespace: openshift-logging
data:
  BUCKET_HOST: s3.us-east-2.amazonaws.com
  BUCKET_NAME: loki-bucket-fake-0001
  BUCKET_PORT: "443"
  BUCKET_REGION: us-east-2
---
# Fake AAP ODF bucket configmap (used by aap policy via fromConfigMap)
apiVersion: v1
kind: ConfigMap
metadata:
  name: aap-bucket-odf
  namespace: ansible-automation-platform
data:
  BUCKET_HOST: s3.us-east-2.amazonaws.com
  BUCKET_NAME: aap-bucket-fake-0001
  BUCKET_PORT: "443"
  BUCKET_REGION: us-east-2
---
# Fake ACM Observability bucket configmap (used by acm-observability policy)
apiVersion: v1
kind: ConfigMap
metadata:
  name: acm-observability
  namespace: open-cluster-management-observability
data:
  BUCKET_HOST: s3.us-east-2.amazonaws.com
  BUCKET_NAME: acm-observability-fake-0001
  BUCKET_PORT: "443"
  BUCKET_REGION: us-east-2
---
# Fake Tempo ODF bucket configmap (used by servicemesh3-ambient policy via fromConfigMap)
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-bucket-odf
  namespace: tracing-system
data:
  BUCKET_HOST: s3.us-east-2.amazonaws.com
  BUCKET_NAME: tempo-bucket-fake-0001
  BUCKET_PORT: "443"
  BUCKET_REGION: us-east-2
