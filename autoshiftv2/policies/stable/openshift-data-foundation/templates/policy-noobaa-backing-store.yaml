# This policy configures the NooBaa backing store for ODF on the managed clusters.
#
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-noobaa-backing-store
  namespace: {{ .Values.policy_namespace }}
  annotations:
    policy.open-cluster-management.io/standards: NIST SP 800-53
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
spec:
{{ if (($.Values.autoshift).dryRun) }}
  remediationAction: inform
{{ end }}
  dependencies:
    - name: policy-storage-cluster-test
      namespace: {{ .Values.policy_namespace }}
      apiVersion: policy.open-cluster-management.io/v1
      compliance: Compliant
      kind: Policy
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: noobaa-backing-store
        spec:
          remediationAction: enforce # will be overridden by remediationAction in parent policy
          severity: high
          evaluationInterval:
            compliant: {{ ((($.Values.autoshift).evaluationInterval).compliant) | default "10m" }}
            noncompliant: {{ ((($.Values.autoshift).evaluationInterval).noncompliant) | default "30s" }}
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: noobaa.io/v1alpha1
                kind: BackingStore
                metadata:
                  name: noobaa-default-backing-store
                  namespace: {{ .Values.odf.namespace }}
                spec:
                  pvPool:
                    numVolumes: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/odf-noobaa-store-size" | default "{{ .Values.odf.noobaaStoreNumVolumes }}" {{ "hub}}" }}'
                    resources:
                      limits:
                        cpu: 1000m
                        memory: 4000Mi
                      requests:
                        cpu: 500m
                        memory: 500Mi
                        storage: '{{ "{{hub" }} index .ManagedClusterLabels "autoshift.io/odf-noobaa-store-num-volumes" | default "{{ .Values.odf.noobaaStoreSize }}" {{ "hub}}" }}'
                    secret: {}
                  type: pv-pool
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-noobaa-backing-store
  namespace: {{ .Values.policy_namespace }}
spec:
  clusterSets:
  {{- range $clusterSet, $value := $.Values.managedClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  {{- range $clusterSet, $value := $.Values.hubClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/odf-noobaa-pvpool'
              operator: In
              values:
              - 'true'
            - key: 'autoshift.io/odf'
              operator: In
              values:
              - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: placement-policy-noobaa-backing-store
  namespace: {{ .Values.policy_namespace }}
placementRef:
  name: placement-policy-noobaa-backing-store
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: policy-noobaa-backing-store
    apiGroup: policy.open-cluster-management.io
    kind: Policy
