# Default values for node-maintenance
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
policy_namespace: open-cluster-policies
nodeMaintenance:
  name: node-maintenance-operator
  namespace: openshift-workload-availability
  channel: stable
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  operatorGroupName: node-maintenance-operator-group
  # targetNamespaces: # Optional: specify target namespaces for namespace-scoped operators
  #   - openshift-workload-availability
  
# hubClusterSets:
#   hub:
#     labels:
#       test1: 'test'
# managedClusterSets:
#   managed:
#     labels:
#       test4: 'test'

### AutoShift Labels Documentation
# The following labels can be set at the cluster or clusterset level to configure this policy:
#
# Enable/Disable:
# node-maintenance<bool>: 'true' or 'false' - Controls whether node-maintenance is managed
#
# Configuration:
# node-maintenance-subscription-name<string>: Operator subscription name (default: 'node-maintenance-operator')
# node-maintenance-channel<string>: Operator channel (default: 'stable')
# node-maintenance-version<string>: Specific operator version (CSV) to install (optional)
# node-maintenance-source<string>: Operator catalog source (default: 'redhat-operators')
# node-maintenance-source-namespace<string>: Catalog namespace (default: 'openshift-marketplace')
#
# Examples:
# autoshift.io/node-maintenance: 'true'
# autoshift.io/node-maintenance-subscription-name: 'node-maintenance-operator'
# autoshift.io/node-maintenance-channel: 'stable'
# autoshift.io/node-maintenance-version: 'operator-name.v1.x.x'
# autoshift.io/node-maintenance-source: 'redhat-operators'
# autoshift.io/node-maintenance-source-namespace: 'openshift-marketplace'