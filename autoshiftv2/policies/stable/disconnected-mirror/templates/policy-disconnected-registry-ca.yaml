# Policy to configure registry CA trust on managed clusters
# Creates a ConfigMap in openshift-config with the mirror registry CA
# and patches image.config.openshift.io/cluster to reference it
# Reads CA from rendered-config ConfigMaps via hub template lookup
{{- $policyNamespace := $.Values.policy_namespace }}
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-disconnected-registry-ca
  namespace: {{ $policyNamespace }}
  annotations:
    policy.open-cluster-management.io/standards: NIST SP 800-53
    policy.open-cluster-management.io/categories: CM Configuration Management
    policy.open-cluster-management.io/controls: CM-2 Baseline Configuration
spec:
{{ if (($.Values.autoshift).dryRun) }}
  remediationAction: inform
{{ end }}
  disabled: false
  hubTemplateOptions:
    serviceAccountName: autoshift-policy-service-account
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: disconnected-registry-ca-bundle
        spec:
          remediationAction: enforce
          severity: high
          evaluationInterval:
            compliant: {{ ((($.Values.autoshift).evaluationInterval).compliant) | default "10m" }}
            noncompliant: {{ ((($.Values.autoshift).evaluationInterval).noncompliant) | default "30s" }}
          object-templates-raw: |
            {{ "{{hub" }} $cm := (lookup "v1" "ConfigMap" "{{ $policyNamespace }}" (printf "%s.rendered-config" .ManagedClusterName)) | default dict {{ "hub}}" }}
            {{ "{{hub" }} $config := (index $cm "data" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $configYaml := (index $config "config" | default "" | fromYaml | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $disconnected := (index $configYaml "disconnected" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $mirror := (index $disconnected "mirrorRegistry" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $mirrorHost := (index $mirror "host") {{ "hub}}" }}
            {{ "{{hub" }} $ca := (index $mirror "ca" | default "") {{ "hub}}" }}
            {{ "{{hub" }} $caRef := (index $mirror "caRef" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} if not (empty $caRef) {{ "hub}}" }}
            {{ "{{hub" }} $refNs := (index $caRef "namespace" | default "{{ $policyNamespace }}") {{ "hub}}" }}
            {{ "{{hub" }} $refCM := (lookup "v1" "ConfigMap" $refNs (index $caRef "name")) | default dict {{ "hub}}" }}
            {{ "{{hub" }} $refData := (index $refCM "data" | default dict) {{ "hub}}" }}
            {{ "{{hub" }} $ca = (index $refData (index $caRef "key") | default $ca) {{ "hub}}" }}
            {{ "{{hub" }} end {{ "hub}}" }}
            {{ "{{hub" }} if and (gt (len $mirrorHost) 0) (gt (len $ca) 0) {{ "hub}}" }}
            {{ "{{hub" }} $registryKey := (replace ":" ".." $mirrorHost) {{ "hub}}" }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: ConfigMap
                metadata:
                  name: autoshift-registry-ca
                  namespace: openshift-config
                data:
                  {{ "{{hub" }} $registryKey {{ "hub}}" }}: |
                    {{ "{{hub" }} $ca | autoindent {{ "hub}}" }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: config.openshift.io/v1
                kind: Image
                metadata:
                  name: cluster
                spec:
                  additionalTrustedCA:
                    name: autoshift-registry-ca
            {{ "{{hub" }} end {{ "hub}}" }}
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-policy-disconnected-registry-ca
  namespace: {{ $policyNamespace }}
spec:
  clusterSets:
  {{- range $clusterSet, $value := .Values.managedClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  {{- range $clusterSet, $value := .Values.hubClusterSets }}
    - {{ $clusterSet }}
  {{- end }}
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - key: 'autoshift.io/disconnected-mirror'
              operator: In
              values:
              - 'true'
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: placement-policy-disconnected-registry-ca
  namespace: {{ $policyNamespace }}
placementRef:
  name: placement-policy-disconnected-registry-ca
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
  - name: policy-disconnected-registry-ca
    apiGroup: policy.open-cluster-management.io
    kind: Policy
