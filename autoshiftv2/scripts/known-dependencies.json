{
  "_comment": "Manual dependencies for operators that don't declare olm.package.required in the catalog. The script will recursively resolve dependencies from here.",
  "odf-operator": [
    "odf-dependencies"
  ],
  "advanced-cluster-management": [
    "multicluster-engine"
  ]
}
