# Default values for portworx
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
policy_namespace: open-cluster-policies
portworx:
  name: portworx
  namespace: portworx
  channel: stable
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  operatorGroupName: portworx-operator-group
  # targetNamespaces: # Optional: specify target namespaces for namespace-scoped operators
  #   - portworx
  
# hubClusterSets:
#   hub:
#     labels:
#       test1: 'test'
# managedClusterSets:
#   managed:
#     labels:
#       test4: 'test'

### AutoShift Labels Documentation
# The following labels can be set at the cluster or clusterset level to configure this policy:
#
# Enable/Disable:
# portworx<bool>: 'true' or 'false' - Controls whether portworx is managed
#
# Configuration:
# portworx-subscription-name<string>: Operator subscription name (default: 'portworx')
# portworx-channel<string>: Operator channel (default: 'stable')
# portworx-version<string>: Specific operator version (CSV) to install (optional)
# portworx-source<string>: Operator catalog source (default: 'redhat-operators')
# portworx-source-namespace<string>: Catalog namespace (default: 'openshift-marketplace')
#
# Examples:
# autoshift.io/portworx: 'true'
# autoshift.io/portworx-subscription-name: 'portworx'
# autoshift.io/portworx-channel: 'stable'
# autoshift.io/portworx-version: 'operator-name.v1.x.x'
# autoshift.io/portworx-source: 'redhat-operators'
# autoshift.io/portworx-source-namespace: 'openshift-marketplace'